---
phase: 01-foundation-money-legal-cores
plan: 02
type: tdd
wave: 1
depends_on: ["01-01"]
files_modified:
  - packages/shared/src/money/money.ts
  - packages/shared/src/money/money.spec.ts
  - packages/shared/src/money/index.ts
  - packages/shared/src/fx/fx.math.ts
  - packages/shared/src/fx/fx.math.spec.ts
  - packages/shared/src/fx/index.ts
  - packages/shared/src/premium/premium.ts
  - packages/shared/src/premium/premium.spec.ts
  - packages/shared/src/premium/index.ts
autonomous: true
requirements: [PLAT-03, FX-01, FX-02, FX-03]
user_setup: []

must_haves:
  truths:
    - "Money is represented with big.js end to end; a precision case that drifts as a JS number is exact as Big"
    - "THB→LAK = (sourceRate + 15 kips per rate unit) × premium, rounded UP (ceil) to whole kip"
    - "On the TH-collection path there is NO conversion and NO markup — the THB amount passes through unchanged (satang preserved)"
    - "Premium is a table-driven THB lookup returning a Big; an unknown key throws"
  artifacts:
    - path: "packages/shared/src/money/money.ts"
      provides: "Money value object over Big (THB satang / LAK whole-kip), no JS number"
      contains: "Big"
    - path: "packages/shared/src/fx/fx.math.ts"
      provides: "quote(market, premiumThb, sourceRatePerThb) with direction rule + ceil markup"
      exports: ["quote"]
    - path: "packages/shared/src/premium/premium.ts"
      provides: "table-driven Premium lookup returning Big THB"
      exports: ["lookupPremiumThb"]
  key_links:
    - from: "packages/shared/src/fx/fx.math.ts"
      to: "big.js Big.roundUp"
      via: "ceil rounding at the FX-quote boundary only"
      pattern: "Big\\.roundUp"
    - from: "packages/shared/src/fx/fx.math.ts"
      to: "MARKUP_KIPS_PER_RATE_UNIT"
      via: "markup applied to the per-THB rate, never to the THB amount or LAK total"
      pattern: "MARKUP_KIPS_PER_RATE_UNIT"
---

<objective>
TDD the money/FX core in packages/shared: a `Money` value object over big.js (PLAT-03), the
FX `quote` function implementing the load-bearing rule — THB→LAK at source rate +15 kips per
rate unit then ceil, with the direction rule (no conversion/markup on the TH-collection path)
(FX-02/FX-03) — and the table-driven Premium lookup (FX-01). All pure, no I/O, exhaustively
vector-tested.

Purpose: A wrong Premium or FX value is real money/legal exposure. This is the most
correctness-critical money math in the platform and must be proven by vector tables before any
boundary or persistence exists.

Output: Three pure modules under packages/shared/src/{money,fx,premium}, each with a colocated
spec and a barrel index, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-money-legal-cores/01-CONTEXT.md
@.planning/phases/01-foundation-money-legal-cores/01-RESEARCH.md
@docs/ENGINEERING-STANDARDS.md
@docs/GLOSSARY.md

<interfaces>
From packages/shared/src/types/index.ts (created in plan 01-01):
  export type Currency = "THB" | "LAK";
  export type Market = "TH" | "LA";

@super-care/shared package.json (01-01) reserves the subpaths `./money`, `./fx`, `./premium`
— each resolves to ./dist/<dir>/index.js. This plan creates those index.ts barrels.
big.js rounding constants (VERIFIED in 01-RESEARCH): Big.roundUp = 3 = ceil for positive money.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Money value object over big.js (PLAT-03)</name>
  <files>packages/shared/src/money/money.ts, packages/shared/src/money/money.spec.ts, packages/shared/src/money/index.ts</files>
  <read_first>
    - .planning/phases/01-foundation-money-legal-cores/01-RESEARCH.md (Pitfall 1 float drift; big.js Money Math; Money { amount: Big; currency } value object; THB 2-dp satang vs LAK whole-kip)
    - .planning/phases/01-foundation-money-legal-cores/01-CONTEXT.md (Money representation decision: Decimal everywhere via big.js, JS number never used for money, one model handles THB satang + LAK whole kip)
    - docs/GLOSSARY.md (Currency/Market terms), docs/ENGINEERING-STANDARDS.md (§A3 TDD, §A4 deep module surface)
    - packages/shared/src/types/index.ts (Currency union to reuse)
  </read_first>
  <behavior>
    - A Money carries a Big `amount` and a glossary `Currency`; constructing from a decimal string is exact
    - A no-float-drift case: building 0.1 + 0.2 worth of money equals exactly 0.3 (would fail as JS number)
    - Money exposes scale awareness: THB allows 2 dp (satang); LAK is whole kip (0 dp) — a LAK money with a fractional sub-kip is either rejected or normalized per a documented rule
    - Money never accepts a JS `number` as the amount source for money (string/Big only) — passing a number is a type error / rejected
  </behavior>
  <action>
    RED first: write money.spec.ts asserting the behaviors above (use a precision case that drifts
    as `number` but is exact as `Big`). Then implement money.ts: a `Money` value object wrapping
    `Big` with a `Currency` from packages/shared types; constructors accept decimal strings or Big,
    NOT JS number (keep the public surface narrow per §A4). Encode each currency's scale (THB 2 dp
    satang, LAK 0 dp whole kip) as documented metadata and apply it only where the rule requires;
    do NOT assume 2 dp everywhere. Do NOT perform FX or rounding-for-conversion here — that lives
    in the fx module. Create money/index.ts re-exporting the public surface. No `any`, no
    `parseFloat`/`.toFixed` on currency.
  </action>
  <verify>
    <automated>pnpm --filter @super-care/shared exec vitest run src/money</automated>
  </verify>
  <acceptance_criteria>
    - `vitest run src/money` exits 0
    - money.spec.ts contains a no-float-drift assertion (a case exact as Big, lossy as number)
    - money.ts imports `Big` from big.js and exposes no constructor path that takes a JS `number` amount
    - `grep -n "parseFloat\|toFixed" packages/shared/src/money/money.ts` finds nothing
  </acceptance_criteria>
  <done>Money value object is Big-backed, scale-aware per currency, refuses float/number money, and its spec (incl. a no-drift case) is green.</done>
</task>

<task type="tdd">
  <name>Task 2: FX quote — direction rule + per-rate markup + ceil (FX-02/FX-03)</name>
  <files>packages/shared/src/fx/fx.math.ts, packages/shared/src/fx/fx.math.spec.ts, packages/shared/src/fx/index.ts</files>
  <read_first>
    - .planning/phases/01-foundation-money-legal-cores/01-RESEARCH.md (FX Math Test Vector Table FX-a..FX-d, TH-a/TH-b, drift; FxQuoteResult shape; Code Examples "FX math — direction rule + markup + ceil"; Pitfall 2; unit note on +15 kips per rate unit — ASSUMED A1)
    - .planning/phases/01-foundation-money-legal-cores/01-CONTEXT.md (FX +15 kips applied per rate unit then ceil/Big.roundUp; direction rule no-conversion-on-THB)
    - docs/GLOSSARY.md (FxQuote, Market, Currency)
    - packages/shared/src/money/money.ts (Money value object from Task 1 — collected amount uses it / a Big)
  </read_first>
  <behavior>
    - LA market, premiumThb=1000, sourceRate=250 → collected 265000 LAK (rate+15, ×premium, exact)
    - LA market, premiumThb=1000, sourceRate=250.0001 → 265000.1 ceils to 265001 LAK (round UP, not round/floor)
    - LA market, premiumThb=1, sourceRate=250.5 → 265.5 ceils to 266 LAK (sub-unit forces ceil to whole kip)
    - TH market, premiumThb=1000 → collected 1000 THB, no conversion, no markup, rate fields null (direction rule)
    - TH market, premiumThb=1499.50 → 1499.50 THB unchanged (satang preserved; markup never applied)
    - drift case: a premium/rate combination that would lose precision as float is exact via Big
    - LA market with a null source rate THROWS (cannot quote LAK without a rate)
  </behavior>
  <action>
    RED first: write fx.math.spec.ts as a vector table driving every row of the research FX table
    (FX-a..FX-d, TH-a, TH-b, drift) plus the null-rate-throws case. Then implement fx.math.ts per
    the research Code Example: export `MARKUP_KIPS_PER_RATE_UNIT = new Big("15")` (mark with the
    A1 unit-assumption comment — magnitude pending rate-feed confirmation; shape is locked),
    `FxQuoteResult` (market, premiumThb, sourceRatePerThb|null, adjustedRatePerThb|null,
    collected{amount,currency}), and `quote(market, premiumThb, sourceRatePerThb)`. TH branch:
    return premiumThb unchanged as THB with null rate fields (NO markup, NO conversion). LA branch:
    `adjusted = sourceRate.plus(MARKUP_KIPS_PER_RATE_UNIT)`, `lak = premiumThb.times(adjusted)
    .round(0, Big.roundUp)`; throw if sourceRate is null. The markup is applied to the per-THB rate
    ONLY — never to the THB amount or the LAK total. `Big.roundUp` is the ONLY rounding and it
    lives ONLY here. Create fx/index.ts barrel. No `any`, no `Math.round`/`Math.floor`, no float.
  </action>
  <verify>
    <automated>pnpm --filter @super-care/shared exec vitest run src/fx</automated>
  </verify>
  <acceptance_criteria>
    - `vitest run src/fx` exits 0 with every research FX vector row asserted (FX-a..FX-d, TH-a, TH-b, drift, null-rate-throws)
    - fx.math.ts uses `Big.roundUp` and contains no `Math.round`/`Math.floor`
    - The TH-path test asserts the returned amount equals premiumThb exactly with null rate fields (no markup)
    - The round-up test (250.0001 → 265001) proves ceil, not round-half / floor
    - `MARKUP_KIPS_PER_RATE_UNIT` is defined once in fx.math.ts and referenced from there only
  </acceptance_criteria>
  <done>FX quote implements direction rule + per-rate +15 markup + ceil, every research vector (incl. TH passthrough, round-up, drift, null-rate-throw) is green, and rounding lives only in the FX boundary.</done>
</task>

<task type="tdd">
  <name>Task 3: Table-driven Premium lookup in THB (FX-01)</name>
  <files>packages/shared/src/premium/premium.ts, packages/shared/src/premium/premium.spec.ts, packages/shared/src/premium/index.ts</files>
  <read_first>
    - .planning/phases/01-foundation-money-legal-cores/01-RESEARCH.md (Premium Lookup section + Code Example; Assumption A2 — amounts/keys are placeholders, lookup SHAPE is what Phase 1 locks)
    - .planning/phases/01-foundation-money-legal-cores/01-CONTEXT.md (table-driven Premium, no underwriting engine)
    - docs/GLOSSARY.md (Premium definition)
    - packages/shared/src/money/money.ts (return type may be Big or Money — match the project convention chosen in Task 1)
  </read_first>
  <behavior>
    - Every row in the premium table resolves and returns a Big THB amount (not a JS number)
    - An unknown (coverage, vehicleType) key throws a clear error
    - The returned value is a Big/decimal, never a float
  </behavior>
  <action>
    RED first: write premium.spec.ts asserting every placeholder row resolves to the expected Big,
    an unknown key throws, and the return is a Big (not number). Then implement premium.ts per the
    research Code Example: a `CoverageClass` union, a readonly `PREMIUM_TABLE` of placeholder rows
    (mark clearly with the A2 placeholder comment — real rate-card data is deferred, NOT a v1
    reduction; the shape and test discipline are the locked deliverable), and
    `lookupPremiumThb(coverage, vehicleType): Big` that finds the row and throws on miss. Create
    premium/index.ts barrel. No `any`, no float.
  </action>
  <verify>
    <automated>pnpm --filter @super-care/shared exec vitest run src/premium</automated>
  </verify>
  <acceptance_criteria>
    - `vitest run src/premium` exits 0
    - premium.spec.ts asserts an unknown key throws and that the returned value is a Big
    - PREMIUM_TABLE rows are decimal strings parsed to Big (no JS number literals for amounts)
    - A comment marks the table data as a deferred-rate-card placeholder (shape locked, not a reduced v1)
  </acceptance_criteria>
  <done>Premium lookup is a pure table-driven function returning Big THB, throwing on unknown keys, with placeholder data explicitly flagged and the lookup shape + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → pure money/FX functions | In-process function calls; no network, no PII, no persistence in Phase 1. The "threat" is value-correctness of money math. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | FX rounding direction | mitigate | Use `Big.roundUp` (ceil) only; a vector test (250.0001→265001) fails if round-half/floor is used. |
| T-02-02 | Tampering | FX markup placement | mitigate | Markup added to the per-THB rate only; TH-passthrough test fails if markup leaks to the THB path; LAK-total test fails if markup is added to the total. |
| T-02-03 | Tampering | float money drift | mitigate | Big end-to-end; explicit no-float-drift cases in money + fx specs; lint/grep forbids parseFloat/toFixed/number money. |
| T-02-04 | Tampering | wrong/missing rate on LAK path | mitigate | `quote` throws when LA market has a null source rate (tested), preventing a silent ~unit-scale error. |
</threat_model>

<verification>
- `pnpm --filter @super-care/shared exec vitest run src/money src/fx src/premium` exits 0.
- Every research FX vector row (incl. TH passthrough, ceil, drift, null-rate-throw) is asserted.
- No `Math.round`/`Math.floor`/`parseFloat`/`toFixed`/number-money anywhere in the three modules.
</verification>

<success_criteria>
- Money is Big end to end (PLAT-03); no-float-drift proven.
- FX quote: +15 per rate unit, ceil, direction rule (FX-02/FX-03) — full vector table green.
- Premium is a table-driven THB lookup returning Big, throws on unknown key (FX-01).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-money-legal-cores/01-02-SUMMARY.md` when done.
</output>
