---
phase: 01-foundation-money-legal-cores
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - tsconfig.base.json
  - tsconfig.json
  - eslint.config.mjs
  - .prettierrc
  - vitest.config.ts
  - .github/workflows/ci.yml
  - .gitignore
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - packages/shared/src/types/index.ts
  - packages/shared/src/schemas/index.ts
  - packages/shared/src/schemas/fx-quote.schema.ts
  - packages/shared/src/schemas/fx-quote.spec.ts
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/src/main.ts
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/src/index.ts
autonomous: false
requirements: [PLAT-01, PLAT-02, PLAT-04]
user_setup: []

must_haves:
  truths:
    - "pnpm workspace typechecks under strict TS with `any` banned"
    - "Vitest runs across packages/shared and reports per-area results"
    - "CI runs typecheck + lint + test and goes red on any failure"
    - "A committed `any` fails lint; a float-money helper has no path into the money layer"
    - "Zod boundary-schema pattern exists in packages/shared/schemas and parses/rejects sample shapes"
  artifacts:
    - path: "tsconfig.base.json"
      provides: "strict TS base (strict, noImplicitAny, noUncheckedIndexedAccess, composite)"
      contains: "noImplicitAny"
    - path: "eslint.config.mjs"
      provides: "flat config banning explicit any"
      contains: "no-explicit-any"
    - path: ".github/workflows/ci.yml"
      provides: "CI gate: install -> typecheck -> lint -> test"
      contains: "vitest"
    - path: "packages/shared/package.json"
      provides: "@super-care/shared workspace package + exports subpaths"
      contains: "@super-care/shared"
    - path: "packages/shared/src/schemas/fx-quote.schema.ts"
      provides: "Zod boundary schema scaffold (PLAT-02 pattern)"
      contains: "z.object"
  key_links:
    - from: "tsconfig.json (root solution)"
      to: "packages/shared, apps/api, apps/web"
      via: "project references"
      pattern: "references"
    - from: ".github/workflows/ci.yml"
      to: "root package.json scripts"
      via: "pnpm typecheck && pnpm lint && pnpm test"
      pattern: "pnpm (typecheck|lint|test)"
---

<objective>
Stand up the pnpm-workspace monorepo spine: strict TypeScript with `any` banned, ESLint flat
config + Prettier, a single Vitest config, two typecheck-clean app shells, the
`@super-care/shared` package skeleton (with subpath `exports` reserved for the pure-core
modules built in Wave 1), the Zod boundary-schema pattern, and a GitHub Actions CI gate that
runs typecheck + lint + test and is red on any failure.

Purpose: This is the Phase-1 walking skeleton (toolchain + CI green). Every Wave-1 pure-core
plan adds files under `packages/shared/src/<domain>/` and depends on this scaffold existing.
NO database, NO HTTP behavior, NO UI behavior, NO adapters — app folders are shells only.

Output: A repo where `pnpm typecheck && pnpm lint && pnpm test` runs green, and CI runs the
same and fails red on a planted failure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-money-legal-cores/01-CONTEXT.md
@.planning/phases/01-foundation-money-legal-cores/01-RESEARCH.md
@.planning/phases/01-foundation-money-legal-cores/01-VALIDATION.md
@docs/ENGINEERING-STANDARDS.md
@docs/GLOSSARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold workspace, strict TS project references, and the two app shells</name>
  <files>pnpm-workspace.yaml, package.json, tsconfig.base.json, tsconfig.json, .gitignore, packages/shared/package.json, packages/shared/tsconfig.json, packages/shared/src/index.ts, packages/shared/src/types/index.ts, apps/api/package.json, apps/api/tsconfig.json, apps/api/src/main.ts, apps/web/package.json, apps/web/tsconfig.json, apps/web/src/index.ts</files>
  <read_first>
    - .planning/phases/01-foundation-money-legal-cores/01-RESEARCH.md (Pattern 1: pnpm workspace + project references; Recommended Project Structure; Installation block; Assumption A5 + A7 on tsc -b and minimal shells)
    - .planning/phases/01-foundation-money-legal-cores/01-CONTEXT.md (Monorepo tooling decision: plain pnpm workspaces + TS project refs, NO Turborepo/Nx)
    - docs/GLOSSARY.md (domain terms — type names must use exact glossary words: Market, OrderState, Currency)
    - existing .gitignore at repo root (extend, do not clobber unrelated entries)
  </read_first>
  <action>
    Create pnpm-workspace.yaml with package globs `packages/*` and `apps/*`. Create root
    package.json named `super-care`, `private: true`, `type: module`, with scripts: `build`
    = `tsc -b`, `typecheck` = `tsc -b` (canonical typecheck for project-reference repos per
    research; verify the exact flag against typescript@6 at implementation per Assumption A5),
    `lint` = `eslint .`, `format` = `prettier --check .`, `format:write` = `prettier --write .`,
    `test` = `vitest run`. Install root dev tooling exactly per the research Installation block
    (typescript@6.0.3, vitest@4.1.8, @vitest/coverage-v8@4.1.8, eslint@10.4.1,
    typescript-eslint@8.60.1, prettier@3.8.3, eslint-config-prettier, @types/node) with
    `pnpm add -D -w`. Commit pnpm-lock.yaml. Create tsconfig.base.json with the exact strict
    options from research Pattern 1 (strict, noImplicitAny, noUncheckedIndexedAccess,
    exactOptionalPropertyTypes, module/moduleResolution NodeNext, target ES2023, declaration,
    composite, skipLibCheck, esModuleInterop, forceConsistentCasingInFileNames). Create root
    solution tsconfig.json with `files: []` and `references` to packages/shared, apps/api,
    apps/web. Create packages/shared as `@super-care/shared` (composite tsconfig extending base,
    rootDir src/outDir dist) with an `exports` map in package.json reserving the subpaths the
    Wave-1 plans will fill: `.`, `./money`, `./fx`, `./premium`, `./commission`, `./order`,
    `./validators`, `./schemas`, `./types` (each → ./dist/<dir>/index.js + types). Create
    packages/shared/src/index.ts (re-export from ./types and ./schemas only for now — do NOT
    reference money/fx/order/etc yet; those subpaths are reserved in exports but their files
    arrive in Wave 1). Create packages/shared/src/types/index.ts exporting the glossary-named
    domain unions: `Currency` = "THB" | "LAK", `Market` = "TH" | "LA". Scaffold apps/api as a
    valid TS project: package.json depending on `@super-care/shared: workspace:*`, tsconfig
    extending base with a reference to packages/shared, and src/main.ts as a no-op bootstrap
    placeholder (no NestJS runtime import — shell only per Assumption A7). Scaffold apps/web
    identically with src/index.ts as a no-op placeholder. Extend .gitignore to add dist/,
    node_modules/, coverage/, *.tsbuildinfo. Use NO `any` anywhere.
  </action>
  <verify>
    <automated>pnpm install && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` completes and pnpm-lock.yaml is committed
    - `pnpm typecheck` exits 0 across packages/shared, apps/api, apps/web (project refs resolve)
    - `packages/shared/package.json` name is `@super-care/shared` and `exports` lists the nine reserved subpaths
    - `tsconfig.base.json` contains `"noImplicitAny": true` and `"composite": true`
    - apps/api and apps/web contain no NestJS/Next runtime imports (shells only)
    - `grep -rn "\bany\b" packages/shared/src apps/api/src apps/web/src` finds no `: any` annotations
  </acceptance_criteria>
  <done>Workspace installs, all three projects typecheck under strict TS, app folders are logic-free shells, and the @super-care/shared exports map reserves every Wave-1 module subpath.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ESLint (ban any) + Prettier + Vitest config + Zod boundary-schema scaffold</name>
  <files>eslint.config.mjs, .prettierrc, vitest.config.ts, packages/shared/src/schemas/index.ts, packages/shared/src/schemas/fx-quote.schema.ts, packages/shared/src/schemas/fx-quote.spec.ts</files>
  <read_first>
    - .planning/phases/01-foundation-money-legal-cores/01-RESEARCH.md (State of the Art: ESLint flat config + unified typescript-eslint; Anti-Patterns: `any` banned via @typescript-eslint/no-explicit-any: error; Validation Architecture: Vitest config is a Wave 0 gap)
    - .planning/phases/01-foundation-money-legal-cores/01-VALIDATION.md (Test Infrastructure, Sampling Rate, no watch-mode flags — CI runs `vitest run` not `vitest`)
    - docs/GLOSSARY.md (FxQuote definition — locked, time-stamped FX quote; Market/Currency terms)
    - .planning/phases/01-foundation-money-legal-cores/01-RESEARCH.md (FxQuoteResult shape in Code Examples — schema fields mirror it: market, premiumThb, sourceRatePerThb, adjustedRatePerThb, collected{amount,currency})
  </read_first>
  <behavior>
    - fxQuoteSchema parses a valid LA quote object (market LA, decimal-string amounts, currency LAK) into a typed value
    - fxQuoteSchema parses a valid TH quote (market TH, currency THB, null rate fields)
    - fxQuoteSchema REJECTS an object with a JS-number money field (money must be string/Decimal-safe, never float) — proves the boundary refuses float money
    - fxQuoteSchema REJECTS an unknown market value and a missing required field
  </behavior>
  <action>
    Create eslint.config.mjs as an ESLint 10 flat config using the unified `typescript-eslint`
    package (`tseslint.config(...)`), enabling the recommended TS ruleset and explicitly setting
    `@typescript-eslint/no-explicit-any` to `"error"`; compose `eslint-config-prettier` last to
    disable formatting-conflict rules. Scope linting to `packages/**/src` and `apps/**/src`;
    ignore dist, node_modules, coverage. Create .prettierrc with project formatting defaults
    (Claude's discretion on width/quotes — keep consistent). Create vitest.config.ts at repo
    root covering `packages/shared/src/**/*.spec.ts`, using the v8 coverage provider, with NO
    watch defaults (the `test` script uses `vitest run`). Create the Zod boundary-schema scaffold
    establishing the PLAT-02 pattern: schemas/fx-quote.schema.ts exporting `fxQuoteSchema`
    (z.object) whose money/rate fields are validated as decimal-safe strings (NOT z.number — a
    number money field must fail parse), `market` as a z.enum over the glossary Markets, and
    `collected` as a nested object with amount(string) + currency(enum). Export the inferred type
    via `z.infer`. schemas/index.ts re-exports it. Write fx-quote.spec.ts FIRST (RED) asserting
    the four behaviors above, then confirm GREEN. Do NOT add FX math here — this is the schema
    boundary pattern only; FX computation lands in plan 01-02. No `any`.
  </action>
  <verify>
    <automated>pnpm lint && pnpm --filter @super-care/shared exec vitest run src/schemas</automated>
  </verify>
  <acceptance_criteria>
    - `eslint.config.mjs` sets `@typescript-eslint/no-explicit-any` to `error`
    - Adding a temporary `const x: any = 1;` to any src file makes `pnpm lint` exit non-zero (spot-check then revert)
    - `vitest run src/schemas` exits 0 with the four fx-quote spec cases green
    - fx-quote.spec.ts asserts a number-typed money field is REJECTED by `fxQuoteSchema`
    - `vitest.config.ts` uses `vitest run` semantics (no watch); coverage provider is v8
  </acceptance_criteria>
  <done>Lint bans `any` and is wired into the CI script, Prettier config exists, Vitest runs the schema specs, and the Zod boundary pattern (rejecting float money) is established and tested.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the GitHub Actions CI gate (typecheck + lint + test, red on failure)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-foundation-money-legal-cores/01-RESEARCH.md (Architecture diagram CI flow: pnpm install -> typecheck -> lint -> test; Environment Availability: GitHub Actions ubuntu-latest, Node v22, pnpm 10.33.2; Security Domain: no secrets in ci.yml, commit lockfile)
    - .planning/phases/01-foundation-money-legal-cores/01-VALIDATION.md (Manual-Only Verifications: CI goes red on a failing test — push a branch with a deliberately failing test and confirm the check fails)
    - root package.json scripts (the CI must invoke the same `typecheck`, `lint`, `test` scripts)
  </read_first>
  <action>
    Create .github/workflows/ci.yml triggering on push and pull_request. Single job on
    ubuntu-latest: checkout, setup pnpm (pin 10.x), setup Node 22 with pnpm cache,
    `pnpm install --frozen-lockfile`, then run `pnpm typecheck`, `pnpm lint`, `pnpm test` as
    separate steps so a failure in any one fails the job (red). Use NO secrets and NO
    credentials (Phase 1 has no secret surface per Security Domain). Do not add deploy or
    publish steps. The workflow must exercise the whole project-reference graph (typecheck
    builds shared + both shells) and the full Vitest suite.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); ['pnpm install','pnpm typecheck','pnpm lint','pnpm test'].forEach(s=>{if(!f.includes(s))throw new Error('missing '+s)}); if(/secret/i.test(f))throw new Error('secret reference present'); console.log('ci.yml ok')"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml runs `pnpm install --frozen-lockfile`, then `pnpm typecheck`, `pnpm lint`, `pnpm test` as distinct steps
    - No `secrets.` references and no deploy/publish steps in the workflow
    - Job runs on ubuntu-latest with Node 22 and pnpm cache
  </acceptance_criteria>
  <done>CI workflow committed; it installs from the frozen lockfile and runs typecheck + lint + test, failing the job (red) on any step failure, with no secret usage.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The monorepo scaffold, strict-TS project references, ESLint(`any` banned)+Prettier, Vitest config, the Zod boundary-schema pattern, two app shells, and the GitHub Actions CI gate.</what-built>
  <how-to-verify>
    1. Run locally: `pnpm install && pnpm typecheck && pnpm lint && pnpm test` — all four must succeed.
    2. PLAT-04 red-on-failure proof (manual per 01-VALIDATION.md): create a throwaway branch, add a deliberately failing test (e.g. `expect(1).toBe(2)`) under packages/shared/src, push it, and confirm the CI check FAILS and would block merge. Then delete the branch / revert.
    3. Confirm apps/api and apps/web contain no business logic, no NestJS/Next runtime, no DB, no HTTP, no UI — shells only.
  </how-to-verify>
  <resume-signal>Type "approved" once local gate is green and CI is observed going red on the planted failing test, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → repo/CI | Source and workflow config committed; no runtime input, no PII, no network egress in Phase 1 |
| npm registry → workspace | Third-party packages installed into the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (root tooling + shared deps) | mitigate | All packages are [CITED] from the locked stack and verified on-registry in 01-RESEARCH (Package Legitimacy Audit); pin exact versions; commit pnpm-lock.yaml; CI uses `--frozen-lockfile`. No NEW package is introduced by this plan. |
| T-01-01 | Tampering | money/boundary types | mitigate | Strict TS + `@typescript-eslint/no-explicit-any: error`; Zod boundary schema rejects JS-number money (float-money cannot enter via the boundary pattern). |
| T-01-02 | Information Disclosure | ci.yml | accept→mitigate | Phase 1 has no secrets; ci.yml uses no `secrets.` and no credentials (verified in Task 3). |
| T-01-03 | Tampering | disabled quality gate letting `any`/float-money through | mitigate | CI runs typecheck + lint + test as separate failing steps; lint bans `any`; PLAT-04 red-on-failure proven at the human-verify checkpoint. |
</threat_model>

<verification>
- `pnpm install && pnpm typecheck && pnpm lint && pnpm test` all exit 0 locally.
- A planted `: any` fails lint; a planted failing test makes CI go red (human-verify checkpoint).
- Zod fx-quote schema rejects float money and unknown market.
- apps/api and apps/web are logic-free shells (no DB/HTTP/UI/adapters).
</verification>

<success_criteria>
- Strict-TS pnpm workspace with project references typechecks across shared + both app shells.
- ESLint flat config bans `any`; Prettier configured; Vitest runs the schema specs.
- CI gate runs typecheck + lint + test and is red on failure (PLAT-04).
- `@super-care/shared` exports map reserves all Wave-1 module subpaths so downstream plans only add files.
- PLAT-01, PLAT-02 (pattern), PLAT-04 satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-money-legal-cores/01-01-SUMMARY.md` when done.
</output>
