---
phase: 01-foundation-money-legal-cores
plan: 04
type: tdd
wave: 1
depends_on: ["01-01"]
files_modified:
  - packages/shared/src/order/order.machine.ts
  - packages/shared/src/order/order.machine.spec.ts
  - packages/shared/src/order/index.ts
  - packages/shared/src/commission/commission.ts
  - packages/shared/src/commission/commission.spec.ts
  - packages/shared/src/commission/index.ts
autonomous: true
requirements: [ORD-02, COMM-01, COMM-02]
user_setup: []

must_haves:
  truths:
    - "The Order state machine accepts only legal transitions and THROWS on illegal ones"
    - "The refund path PAID/CERT_FAILED → REFUNDING → REFUNDED is reachable and tested; refund is only reachable after PAID"
    - "All failure states (OCR_FAILED, PAYMENT_FAILED, CERT_FAILED) are modelled and reachable"
    - "Commission is computed on the THB base via a tier ladder, returning a Big"
    - "Commission boundary cases are tested on BOTH sides of every threshold (just-below, exactly-at, just-above)"
  artifacts:
    - path: "packages/shared/src/order/order.machine.ts"
      provides: "headless Order machine + pure next(state,event) that throws on illegal transitions"
      exports: ["orderMachine", "next"]
    - path: "packages/shared/src/commission/commission.ts"
      provides: "tier-ladder commission on THB base returning Big"
      exports: ["computeCommissionThb"]
  key_links:
    - from: "packages/shared/src/order/order.machine.ts"
      to: "illegal transition handling"
      via: "next() throws IllegalTransition"
      pattern: "throw"
    - from: "packages/shared/src/commission/commission.ts"
      to: "big.js"
      via: "commission computed on Big THB base"
      pattern: "Big"
---

<objective>
TDD the Order state machine and the commission tier ladder in packages/shared. The Order
machine is the headless transition table (ORD-02): legal transitions only, illegal transitions
THROW, all failure states (OCR/payment/cert-gen) modelled, and the refund path
(PAID/CERT_FAILED → REFUNDING → REFUNDED) reachable only after PAID. The commission ladder
(COMM-01/COMM-02) computes on the THB base with boundary tests on both sides of every threshold.

Purpose: An illegal Order transition or a wrong-base/boundary commission value is money/legal
exposure. Both are proven pure and exhaustively tested before any service wraps them
(orchestration lands in Phase 4 / Phase 8).

Output: order and commission modules with colocated specs + barrels, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-money-legal-cores/01-CONTEXT.md
@.planning/phases/01-foundation-money-legal-cores/01-RESEARCH.md
@docs/ENGINEERING-STANDARDS.md
@docs/GLOSSARY.md

<interfaces>
@super-care/shared package.json (01-01) reserves subpaths `./order` and `./commission`.
This plan creates packages/shared/src/{order,commission}/* and their index.ts barrels.
xstate@5.32.0 is a dependency of @super-care/shared (installed in 01-01 per research Installation).
big.js Big.roundUp = 3 (ceil). Glossary: Order, Commission, Partner, Premium, Market.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Order state machine — legal transitions only, illegal throw, refund path (ORD-02)</name>
  <files>packages/shared/src/order/order.machine.ts, packages/shared/src/order/order.machine.spec.ts, packages/shared/src/order/index.ts</files>
  <read_first>
    - .planning/phases/01-foundation-money-legal-cores/01-RESEARCH.md (XState v5 Order Machine: full transition table; the next() throw-on-illegal note; Open Question 3 + Assumption A6 — planner picks XState-wrapper vs plain table; State of the Art: transition() pure fn, getNextSnapshot deprecated)
    - .planning/phases/01-foundation-money-legal-cores/01-CONTEXT.md (Order state machine decision: states incl. OCR_FAILED/PAYMENT_FAILED/CERT_FAILED/REFUNDING/REFUNDED; refund only after PAID; illegal transitions throw; XState used headless as a pure transition table)
    - docs/GLOSSARY.md (Order — governed by the order state machine), docs/ENGINEERING-STANDARDS.md (§A3, §A4)
  </read_first>
  <behavior>
    - Happy path is reachable: DRAFT → QUOTED → AWAITING_PAYMENT → PAID → ISSUING_CERTIFICATE → COMPLETED
    - Refund path is reachable from PAID: PAID → REFUNDING → REFUNDED, and from CERT_FAILED: CERT_FAILED → REFUNDING → REFUNDED
    - Refund is NOT reachable before PAID (e.g. INITIATE_REFUND from DRAFT/QUOTED/AWAITING_PAYMENT throws)
    - Failure states reachable: OCR_FAILED (from DRAFT), PAYMENT_FAILED (from AWAITING_PAYMENT), CERT_FAILED (from ISSUING_CERTIFICATE)
    - An illegal transition (e.g. PAYMENT_CAPTURED from DRAFT) THROWS an IllegalTransition error
    - COMPLETED / REFUNDED / CANCELLED are terminal — any event from them throws
  </behavior>
  <action>
    RED first: write order.machine.spec.ts covering the happy path, both refund entries
    (PAID and CERT_FAILED), the refund-before-PAID throws cases, every failure-state transition,
    a representative illegal transition that throws, and a terminal-state event that throws. Then
    implement order.machine.ts. Per research Assumption A6 / Open Question 3, prefer the lowest-risk
    approach: define the canonical machine with xstate `createMachine` using the full transition
    table from research (states DRAFT, OCR_FAILED, QUOTED, AWAITING_PAYMENT, PAYMENT_FAILED, PAID,
    ISSUING_CERTIFICATE, CERT_FAILED, REFUNDING, COMPLETED[final], REFUNDED[final], CANCELLED[final]),
    and export a pure `next(stateValue, eventType): string` that THROWS `IllegalTransition` when the
    event is not defined for the current state (do NOT rely on XState returning same-state silently —
    detect the no-op explicitly, or back next() with a plain TRANSITIONS table derived from the same
    machine config; verify the exact no-op detection against xstate@5.32.0 at implementation). Create
    order/index.ts exporting orderMachine + next. No `any`.
  </action>
  <verify>
    <automated>pnpm --filter @super-care/shared exec vitest run src/order</automated>
  </verify>
  <acceptance_criteria>
    - The spec proves the full happy path and BOTH refund entries (PAID→REFUNDING→REFUNDED and CERT_FAILED→REFUNDING→REFUNDED) reach REFUNDED
    - The spec asserts INITIATE_REFUND before PAID throws, and a representative illegal transition (PAYMENT_CAPTURED from DRAFT) throws
    - The spec asserts an event from a terminal state (COMPLETED/REFUNDED/CANCELLED) throws
    - `grep -n "throw" packages/shared/src/order/order.machine.ts` confirms next() throws on illegal transitions
    - `vitest run src/order` exits 0
  </acceptance_criteria>
  <done>Order machine models all states incl. failures and refund; next() throws on illegal/terminal transitions; refund reachable only after PAID; all transition specs green.</done>
</task>

<task type="tdd">
  <name>Task 2: Commission tier ladder on the THB base + boundary vectors (COMM-01/COMM-02)</name>
  <files>packages/shared/src/commission/commission.ts, packages/shared/src/commission/commission.spec.ts, packages/shared/src/commission/index.ts</files>
  <read_first>
    - .planning/phases/01-foundation-money-legal-cores/01-RESEARCH.md (Commission Tier Ladder: shape, Code Example, the boundary vector table T0a..Tround; Assumption A3 — tiers/percentages/flats + whole-volume application are placeholders, boundary-test discipline is the locked deliverable)
    - .planning/phases/01-foundation-money-legal-cores/01-CONTEXT.md (commission tier ladder; boundary cases both sides of every threshold; THB base)
    - docs/GLOSSARY.md (Commission, Partner, Premium)
    - packages/shared/src/types/index.ts (Currency) — base is THB
  </read_first>
  <behavior>
    - partnerVolume 0 → tier0 5% of 1000 THB = 50.00
    - partnerVolume 9 (just below threshold 10) → tier0 5% = 50.00
    - partnerVolume 10 (exactly at threshold 10) → tier1 7% = 70.00
    - partnerVolume 49 (just below 50) → tier1 7% = 70.00
    - partnerVolume 50 (exactly at 50) → tier2 10% + 50 flat = 150.00
    - partnerVolume 51 (just above 50) → tier2 = 150.00
    - rounding case: volume 10, premium 333.33, 7% = 23.3331 → 23.33 (documented THB satang rounding)
    - commission is computed on the THB base and returned as a Big (never a JS number)
  </behavior>
  <action>
    RED first: write commission.spec.ts as a vector table driving every row T0a..Tround from
    research, asserting both sides of every threshold (just-below, exactly-at, just-above) and the
    rounding case. Then implement commission.ts per the research Code Example: a readonly `LADDER`
    of placeholder tiers (minVolume, percent, flatThb) — mark with the A3 placeholder comment
    (real policy deferred; boundary discipline + shape locked), and
    `computeCommissionThb(partnerVolume: number, premiumThb: Big): Big` selecting the highest tier
    whose minVolume ≤ partnerVolume (whole-volume application — document this choice), applying
    percent (on the Big THB premium) and/or flatThb, rounding to 2dp THB satang with a documented
    rounding mode. Create commission/index.ts barrel. No `any`, no float.
  </action>
  <verify>
    <automated>pnpm --filter @super-care/shared exec vitest run src/commission</automated>
  </verify>
  <acceptance_criteria>
    - The spec asserts both sides of every threshold (volumes 9/10 around 10, and 49/50/51 around 50)
    - The spec asserts the rounding case (333.33 @ 7% → 23.33) and that the return is a Big
    - LADDER tier amounts are decimal strings → Big (no JS number money literals); placeholder comment present
    - `vitest run src/commission` exits 0
  </acceptance_criteria>
  <done>Commission ladder computes on the Big THB base, returns Big, and passes both-sides-of-every-threshold boundary vectors incl. the rounding case; placeholder policy data flagged, shape locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → pure state machine / commission fn | In-process; no network/PII/persistence. The "threat" is an illegal Order transition slipping through or a wrong-base/boundary commission value. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | illegal Order transition slipping through | mitigate | next() throws IllegalTransition; spec asserts illegal + terminal-state events throw; no-op XState result explicitly detected (not trusted as success). |
| T-04-02 | Tampering | money-taken-no-cover dead-end (refund unreachable) | mitigate | Refund path modelled from PAID and CERT_FAILED and proven reachable to REFUNDED; refund-before-PAID throws. |
| T-04-03 | Tampering | commission on wrong base / off-by-one at threshold / float | mitigate | THB base is explicit; both-sides-of-threshold vectors; Big with documented rounding; no JS number money. |
</threat_model>

<verification>
- `pnpm --filter @super-care/shared exec vitest run src/order src/commission` exits 0.
- Illegal + terminal transitions throw; both refund entries reach REFUNDED; commission boundaries both-sided.
- No float money in commission; next() throws on illegal transitions.
</verification>

<success_criteria>
- Order machine: legal-only transitions, illegal throw, failure states + refund path reachable (ORD-02).
- Commission: THB-base tier ladder returning Big with both-sides-of-every-threshold tests (COMM-01/COMM-02).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-money-legal-cores/01-04-SUMMARY.md` when done.
</output>
