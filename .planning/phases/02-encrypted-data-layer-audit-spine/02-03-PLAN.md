---
phase: 02-encrypted-data-layer-audit-spine
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - .github/workflows/ci.yml
  - .gitleaks.toml
autonomous: false
requirements:
  - SEC-02

must_haves:
  truths:
    - "CI runs a gitleaks secret-scan step using the binary (no licensed action)"
    - "gitleaks exits non-zero (fails the build) on a planted secret"
    - "The existing typecheck/lint/test gate still runs; a CI Postgres service is available for integration tests"
    - ".env.example placeholders and test fixtures are allowlisted (no false positives)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "gitleaks secret-scan job + Postgres service container for integration tests"
      contains: "gitleaks"
    - path: ".gitleaks.toml"
      provides: "default rules + allowlist for .env.example / spec fixtures"
      contains: "allowlist"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "gitleaks binary v8.30.1"
      via: "install + detect --exit-code 1"
      pattern: "gitleaks detect"
---

<objective>
Wire CI secret-scanning into the existing GitHub Actions gate using the gitleaks CLI binary (not the licensed action), with a `.gitleaks.toml` that allowlists `.env.example`/test fixtures, and add a Postgres service container so the Plan-02 integration tests + migration can run in CI. Verify with a human-checked planted-secret run that the gate goes red.

Purpose: Closes SEC-02 success criterion 4 — "CI secret-scanning is wired and fails on a planted secret; secrets resolve from env." Also makes the integration suite runnable in CI by providing Postgres + `prisma migrate deploy`.
Output: gitleaks step + Postgres service in ci.yml; `.gitleaks.toml`; verified red-on-planted-secret.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-encrypted-data-layer-audit-spine/02-RESEARCH.md
@.planning/phases/02-encrypted-data-layer-audit-spine/02-VALIDATION.md
@.planning/phases/02-encrypted-data-layer-audit-spine/USER-SETUP.md
@.github/workflows/ci.yml
@.env.example

<interfaces>
<!-- From 02-RESEARCH § Secret Scanning — use directly. -->

gitleaks binary v8.30.1 (license-free as a CLI; the *action* needs an org GITLEAKS_LICENSE → do NOT use the action).
Install in a workflow step:
  curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/gitleaks_8.30.1_linux_x64.tar.gz | tar -xz -C /usr/local/bin gitleaks
Run: gitleaks detect --source . --redact --exit-code 1 --config .gitleaks.toml  (checkout with fetch-depth: 0 to scan full history)

Existing ci.yml job "ci" runs: install → typecheck → lint → test (pnpm). Integration tests (Plan 02 `pnpm test:int`) need DATABASE_URL + Postgres.
.gitleaks.toml: [extend] useDefault = true; [allowlist] paths for .env.example and *.spec.ts fixtures.
Env secrets (DATABASE_URL/MASTER_KEY_V1/INDEX_KEY) are documented in USER-SETUP.md — for CI integration, provide test values via the Postgres service + GitHub secrets/test keys; never commit them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gitleaks secret-scan + Postgres service to CI</name>
  <files>.github/workflows/ci.yml, .gitleaks.toml</files>
  <read_first>
    - .planning/phases/02-encrypted-data-layer-audit-spine/02-RESEARCH.md (§ Secret Scanning — binary install, --exit-code 1, .gitleaks.toml; Pitfall 4 gitleaks-action license; § Validation Architecture Postgres service container)
    - .github/workflows/ci.yml (current single "ci" job)
    - .env.example (placeholders to allowlist)
    - .planning/phases/02-encrypted-data-layer-audit-spine/USER-SETUP.md (env var names)
  </read_first>
  <action>
    Add a separate `secret-scan` job to ci.yml: checkout with `fetch-depth: 0` (scan full history), install the gitleaks v8.30.1 binary via the curl|tar step, then `gitleaks detect --source . --redact --exit-code 1 --config .gitleaks.toml`. Use the BINARY, never `gitleaks/gitleaks-action` (org license requirement — Pitfall 4). Verify the release asset URL resolves (Assumption A4); if the asset name differs, adjust to the actual v8.30.1 linux x64 asset.
    Create `.gitleaks.toml`: `[extend] useDefault = true`; an `[allowlist]` with `paths` matching `\.env\.example$` and `.*\.spec\.ts$` (and `.int.spec.ts`) so example placeholders and test fixtures do not trip the scan.
    Extend the existing test job (or add an integration step) with a Postgres 17 `services` container exposing 5432, set `DATABASE_URL` to that service, run `pnpm --filter @super-care/web exec prisma migrate deploy` before integration tests, and run `pnpm test:int`. Provide `MASTER_KEY_V1`/`INDEX_KEY`/`MASTER_KEY_CURRENT` as test env (generated non-secret test keys in the workflow env or GitHub secrets — never committed). Keep the existing typecheck/lint/test (DB-free) steps intact and still red-on-failure.
  </action>
  <verify>
    <automated>grep -q "gitleaks detect" .github/workflows/ci.yml && grep -q "useDefault" .gitleaks.toml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml contains a `secret-scan` job that installs the gitleaks binary and runs `gitleaks detect ... --exit-code 1 --config .gitleaks.toml` with `fetch-depth: 0`
    - ci.yml does NOT reference `gitleaks/gitleaks-action`
    - ci.yml has a Postgres 17 service container, sets DATABASE_URL, runs `prisma migrate deploy`, then `pnpm test:int`
    - `.gitleaks.toml` extends defaults and allowlists `.env.example` + spec fixtures
    - existing typecheck/lint/test steps remain
  </acceptance_criteria>
  <done>CI runs license-free gitleaks secret-scanning with an allowlist, and a Postgres service container enables the integration suite + committed-migration deploy in CI.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>A gitleaks secret-scan CI step (binary, `--exit-code 1`) with a `.gitleaks.toml` allowlist, plus a Postgres service container so integration tests run in CI.</what-built>
  <how-to-verify>
    1. On a throwaway branch, plant a fake AWS-style secret in a non-allowlisted source file (e.g. `AKIA` + 16 uppercase/digit chars assigned to a var in a `.ts` file that is NOT a `.spec.ts`).
    2. Push the branch and open the Actions run.
    3. Confirm the `secret-scan` job turns RED (gitleaks detects the planted key, exit code 1 fails the build).
    4. Confirm `.env.example` placeholders and `*.spec.ts` fixtures do NOT trigger findings (no false positives).
    5. Remove the planted secret and delete the throwaway branch; confirm `main` (or the clean branch) goes green.
  </how-to-verify>
  <resume-signal>Type "approved" once the planted secret turned CI red and the clean branch is green, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer commit → repo | A committed secret must be caught before merge |
| CI runner → Postgres service | Ephemeral test DB; test-only keys, never production secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Information Disclosure | Secret committed to the repo | mitigate | gitleaks `--exit-code 1` over full history fails the build; verified by a planted-secret run |
| T-02-13 | Information Disclosure | gitleaks bypass via false-allowlist | mitigate | Allowlist scoped narrowly to `.env.example` + `*.spec.ts`; planted real-file secret still caught (human-verify) |
| T-02-14 | Tampering | CI uses licensed action requiring a secret | accept→mitigate | Use the binary, not the action — no GITLEAKS_LICENSE, no supply-chain license coupling |
| T-02-15 | Information Disclosure | Real secrets leaked into CI logs | mitigate | `--redact`; CI uses test-only keys + ephemeral Postgres; production secrets never in the workflow |
| T-02-SC | Tampering | gitleaks binary download integrity | mitigate | Pinned release v8.30.1 from the official gitleaks GitHub release; URL verified to resolve |
</threat_model>

<verification>
- `grep "gitleaks detect" .github/workflows/ci.yml` present; no `gitleaks-action` reference.
- `.gitleaks.toml` extends defaults + allowlists example/fixtures.
- Human-verify: planted secret turns the `secret-scan` job red; clean branch green; no false positives on `.env.example`/specs.
- CI Postgres service + `prisma migrate deploy` + `pnpm test:int` wired.
</verification>

<success_criteria>
- CI secret-scanning is wired with the license-free binary and fails on a planted secret (SEC-02 exit gate).
- No false positives on documented placeholders/test fixtures.
- Integration suite runs in CI against a Postgres service container with the committed migration deployed.
</success_criteria>

<output>
Create `.planning/phases/02-encrypted-data-layer-audit-spine/02-03-SUMMARY.md` when done
</output>
