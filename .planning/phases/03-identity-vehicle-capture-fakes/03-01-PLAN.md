---
phase: 03-identity-vehicle-capture-fakes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/postcss.config.mjs
  - apps/web/components.json
  - apps/web/next-env.d.ts
  - apps/web/app/layout.tsx
  - apps/web/app/globals.css
  - apps/web/app/[locale]/page.tsx
  - apps/web/app/[locale]/health/page.tsx
  - apps/web/messages/en.json
  - apps/web/src/i18n/request.ts
  - apps/web/src/i18n/routing.ts
  - apps/web/middleware.ts
  - apps/web/src/components/ui/.gitkeep
  - apps/web/playwright.config.ts
  - apps/web/e2e/smoke.spec.ts
  - tsconfig.json
  - package.json
  - eslint.config.mjs
  - .gitignore
  - .github/workflows/ci.yml
autonomous: false
requirements: [UI-01, API-01, PLAT-01, PLAT-04]
user_setup: []

must_haves:
  truths:
    - "apps/web builds and serves as a runnable Next.js App Router app"
    - "Tailwind v4 + shadcn/ui + next-intl render an English page with no hardcoded strings in components"
    - "app/ is both typechecked and linted by the root CI scripts"
    - "Playwright runs a smoke test against the running app"
  artifacts:
    - path: "apps/web/app/layout.tsx"
      provides: "Root layout with NextIntlClientProvider + Thai-capable font stack"
      min_lines: 15
    - path: "apps/web/next.config.ts"
      provides: "Next config wired with next-intl plugin"
    - path: "apps/web/playwright.config.ts"
      provides: "Playwright config booting next for e2e"
    - path: "apps/web/messages/en.json"
      provides: "next-intl en catalog (no hardcoded strings)"
  key_links:
    - from: "package.json typecheck/lint scripts"
      to: "apps/web/app"
      via: "tsc --noEmit + eslint glob covering app/"
      pattern: "app/"
    - from: "apps/web/app/layout.tsx"
      to: "messages/en.json"
      via: "next-intl provider"
      pattern: "NextIntl"
---

<objective>
Convert apps/web from a typecheck-only ESM library (tsc -b, rootDir: src, no app/) into a
runnable Next.js App Router app, install the locked UI stack behind a dependency-vetting
checkpoint, and wire the i18n seam + Playwright. This is the Wave-0 blocking foundation: no
wizard step can be built until the shell runs, app/ is typechecked AND linted, and the
build/serve smoke test is green.

Purpose: Unblock every subsequent Phase 3 plan. The hard part of this phase is the runnable-app
conversion, not the forms (03-RESEARCH.md Summary + Pitfall 1).
Output: A runnable, linted, typechecked Next.js app with Tailwind v4 + shadcn/ui + next-intl +
Playwright, and reconciled root build/lint/CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-identity-vehicle-capture-fakes/03-CONTEXT.md
@.planning/phases/03-identity-vehicle-capture-fakes/03-UI-SPEC.md
@.planning/phases/03-identity-vehicle-capture-fakes/03-RESEARCH.md
@CLAUDE.md
@apps/web/package.json
@apps/web/tsconfig.json
@tsconfig.base.json
@tsconfig.json
@eslint.config.mjs
@vitest.config.ts
@.github/workflows/ci.yml

<interfaces>
<!-- Verified registry versions (03-CONTEXT.md locked + 03-RESEARCH.md). Pin these majors. -->
next 16.2.7 · react/react-dom 19.2.7 · tailwindcss + @tailwindcss/postcss 4.3.0 ·
next-intl 4.13.0 · react-hook-form 7.77 · @hookform/resolvers 5.4 · zod 4.4.3 ·
@playwright/test 1.60 · shadcn canary CLI (lucide-react, sonner pulled by init).

Existing repo facts the executor MUST honor:
- Root scripts: build/typecheck = `tsc -b`; lint = `eslint .`; test = `vitest run`.
- Root tsconfig.json references packages/shared + apps/web as project refs.
- apps/web/tsconfig.json: rootDir:src, outDir:dist, composite:true, extends tsconfig.base.json (NodeNext).
- eslint.config.mjs files glob is `apps/**/src/**/*.ts` only — does NOT cover app/.
- .gitignore already ignores .next/ and .env*; does NOT yet ignore .uploads/.
- Prisma client singleton already exists at apps/web/src/server/db/client.ts (HMR-safe).
- apps/web is "type":"module"; tsconfig.base is module:NodeNext.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Dependency-vetting checkpoint — vet net-new packages before install</name>
  <what-built>
    Nothing yet — this gate runs BEFORE any package install. slopcheck was unavailable during
    research, so every net-new package is tagged [ASSUMED] (03-RESEARCH.md Package Legitimacy
    Audit) and MUST be human-verified on the registry before installation (T-03-SC).
  </what-built>
  <read_first>
    - .planning/phases/03-identity-vehicle-capture-fakes/03-RESEARCH.md (Package Legitimacy Audit + Standard Stack)
    - .planning/phases/03-identity-vehicle-capture-fakes/03-CONTEXT.md (Verified registry versions)
  </read_first>
  <how-to-verify>
    Confirm each package + pinned version is legitimate before install. For each, open the
    registry page and confirm publisher + repo:
      1. next 16.2.7 — npmjs.com/package/next (vercel/next.js)
      2. react / react-dom 19.2.7 — npmjs.com/package/react (facebook/react)
      3. tailwindcss + @tailwindcss/postcss 4.3.0 — npmjs.com/package/tailwindcss
      4. next-intl 4.13.0 — npmjs.com/package/next-intl (amannn/next-intl)
      5. react-hook-form 7.77 — npmjs.com/package/react-hook-form
      6. @hookform/resolvers 5.4 — npmjs.com/package/@hookform/resolvers
      7. lucide-react, sonner — confirm version matches shadcn canary template
      8. @playwright/test 1.60 — npmjs.com/package/@playwright/test
    Reject any package whose publisher/repo does not match the audit table.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms all listed packages + versions are legitimate, OR names substitutions.
    - This checkpoint is NEVER auto-approvable (workflow.auto_advance ignored — supply-chain gate).
  </acceptance_criteria>
  <resume-signal>Type "approved" (packages vetted) or list rejected/substituted packages.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install stack + convert apps/web to a runnable Next.js App Router app</name>
  <files>apps/web/package.json, apps/web/next.config.ts, apps/web/postcss.config.mjs, apps/web/components.json, apps/web/next-env.d.ts, apps/web/tsconfig.json, apps/web/app/layout.tsx, apps/web/app/globals.css, apps/web/app/[locale]/page.tsx, apps/web/app/[locale]/health/page.tsx, apps/web/messages/en.json, apps/web/src/i18n/request.ts, apps/web/src/i18n/routing.ts, apps/web/middleware.ts, apps/web/src/components/ui/.gitkeep, .gitignore</files>
  <read_first>
    - .planning/phases/03-identity-vehicle-capture-fakes/03-RESEARCH.md (Pitfall 1, 2, 3; Recommended Project Structure; Tailwind v4 entry + Thai font; next-intl seam)
    - .planning/phases/03-identity-vehicle-capture-fakes/03-UI-SPEC.md (Visual direction; i18n DECIDED section; App shell)
    - apps/web/tsconfig.json, tsconfig.base.json (current library config to convert)
    - apps/web/src/server/db/client.ts (existing HMR-safe Prisma singleton — do not duplicate)
    - docs/GLOSSARY.md (exact product/term names for any visible label keys)
  </read_first>
  <action>
    Install the vetted stack into apps/web with pnpm, pinning the locked majors (next@16,
    react@19, react-dom@19, next-intl@4, react-hook-form@7, @hookform/resolvers@5, zod@4) and dev
    deps tailwindcss@4 + @tailwindcss/postcss@4 + @playwright/test@1.60. Run `pnpm dlx
    shadcn@canary init` (canary required for Tailwind 4 + React 19 per Pitfall 2); configure
    components.json aliases to the monorepo `@/` path mapping ui→src/components/ui.

    Convert apps/web off the tsc -b library build (Pitfall 1): drop rootDir/outDir/composite emit
    in apps/web/tsconfig.json; set jsx:"preserve", noEmit:true, moduleResolution:"bundler",
    plugins:[{name:"next"}]; include app, src, next-env.d.ts. Add next.config.ts wrapped with the
    next-intl plugin. Add postcss.config.mjs with @tailwindcss/postcss. Add app/globals.css with
    `@import "tailwindcss";` plus shadcn token block. Add app/layout.tsx using next/font with a
    Thai-capable stack (Noto Sans Thai + Inter, Pitfall 3) and NextIntlClientProvider; apply the
    font CSS variables on <html>. Add the next-intl seam: src/i18n/routing.ts (locales:["en"],
    defaultLocale:"en"), src/i18n/request.ts loading messages/en.json, middleware.ts for locale
    routing, and messages/en.json. Add app/[locale]/page.tsx (landing) and
    app/[locale]/health/page.tsx (a "/health" page using useTranslations — proves the i18n seam,
    no hardcoded strings per i18n-check). Add apps/web scripts: dev=`next dev`, build=`next
    build`, start=`next start`. Add .uploads/ to .gitignore (storage adapter writes here in 03-03;
    must never be committed). Create src/components/ui/.gitkeep so shadcn components land there.

    Do NOT serve uploads from public/. Do NOT import any server/db, server/crypto, or repo module
    into a client component (server-only boundary enforced in later plans).
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec next build 2>&1 | tail -5; test -d .next && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @super-care/web build` exits 0 (Next build, not tsc -b).
    - apps/web/app/layout.tsx renders NextIntlClientProvider; no user-facing string is hardcoded
      in app/[locale]/health/page.tsx (all via useTranslations + messages/en.json).
    - .gitignore contains `.uploads/`.
    - shadcn components.json exists; src/components/ui/ exists for generated components.
  </acceptance_criteria>
  <done>apps/web builds and serves; i18n seam returns the en catalog; Thai font stack applied; .uploads/ gitignored.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile root typecheck/lint/CI for app/ + add Playwright smoke</name>
  <files>tsconfig.json, package.json, eslint.config.mjs, apps/web/playwright.config.ts, apps/web/e2e/smoke.spec.ts, .github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/03-identity-vehicle-capture-fakes/03-RESEARCH.md (Pitfall 1 warning signs; Wave 0 Gaps; CI bullet)
    - .planning/phases/03-identity-vehicle-capture-fakes/03-VALIDATION.md (Test Infrastructure; full suite command)
    - eslint.config.mjs (current files glob + ignores)
    - .github/workflows/ci.yml (current jobs: ci / integration-tests / secret-scan)
    - tsconfig.json, package.json (root scripts: build/typecheck = tsc -b)
  </read_first>
  <action>
    Reconcile the root build so app/ is typechecked AND linted (Pitfall 1). Since apps/web is no
    longer a composite tsc -b project, remove apps/web from the root tsconfig.json project
    references and keep packages/shared on tsc -b. Update root package.json `typecheck` to run BOTH
    `tsc -b` (for packages/shared) AND `tsc --noEmit -p apps/web` (Next-aware typecheck of app/ +
    src/). Update root `build` to `tsc -b` (shared) — apps/web build is `next build` via its own
    script. Extend eslint.config.mjs so app/ is linted: add `apps/web/app/**/*.{ts,tsx}` and
    `apps/web/src/**/*.{ts,tsx}` to the files globs (keep the no-explicit-any rule), and add the
    Next ESLint config (eslint-config-next via FlatCompat) so JSX/React rules apply. Keep `.next/`,
    `dist/`, generated prisma excluded.

    Add Playwright: apps/web/playwright.config.ts with a webServer launching `next dev` (or build+
    start in CI) and a chromium project plus a mobile-viewport project (for UI-01). Add
    apps/web/e2e/smoke.spec.ts asserting the app serves a 200 and renders the landing page. Add a
    root `test:e2e` script. Add a Playwright job (or step) to .github/workflows/ci.yml that runs
    `pnpm exec playwright install --with-deps chromium` then the smoke e2e (gate to its own job so
    it doesn't slow the unit job). Extend the CI typecheck/lint steps to cover app/ (they will, via
    the updated root scripts).
  </action>
  <verify>
    <automated>cd /Users/khamphone/Documents/CodingProject/Projects/super-care && pnpm typecheck && pnpm lint && cd apps/web && pnpm exec playwright install chromium >/dev/null 2>&1; pnpm exec playwright test e2e/smoke.spec.ts --reporter=line 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` covers app/ (introduce a deliberate type error in an app/ file → it fails; revert).
    - `pnpm lint` reports on app/ files (eslint glob includes app/).
    - `pnpm --filter @super-care/web exec playwright test e2e/smoke.spec.ts` passes (app serves, landing renders).
    - .github/workflows/ci.yml has a Playwright step with `playwright install`.
  </acceptance_criteria>
  <done>Root typecheck+lint cover app/; Playwright smoke green; CI runs e2e.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo | Net-new packages enter the supply chain at install |
| filesystem → git | .uploads/ encrypted blobs must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | pnpm installs (next/react/tailwind/shadcn/next-intl/rhf/playwright) | mitigate | Task 1 blocking-human dep-vetting checkpoint; pin verified versions; never auto-approve |
| T-03-01 | Information Disclosure | .uploads/ encrypted document blobs | mitigate | Add `.uploads/` to .gitignore in Task 2; storage adapter (03-03) writes outside public/ |
| T-03-02 | Information Disclosure | Server/crypto/Prisma in client bundle | mitigate | server-only boundary established now (no repo/crypto import in app/ client components); enforced in 03-03/03-04 |
</threat_model>

<verification>
- `pnpm --filter @super-care/web build` exits 0.
- `pnpm typecheck && pnpm lint` pass and cover app/.
- Playwright smoke e2e green.
- No hardcoded user-facing strings in shipped pages (i18n via next-intl).
</verification>

<success_criteria>
apps/web is a runnable Next.js App Router app; Tailwind v4 + shadcn + next-intl wired; app/
typechecked + linted by root scripts; Playwright smoke green; .uploads/ gitignored.
</success_criteria>

<output>
Create `.planning/phases/03-identity-vehicle-capture-fakes/03-01-SUMMARY.md` when done.
</output>
