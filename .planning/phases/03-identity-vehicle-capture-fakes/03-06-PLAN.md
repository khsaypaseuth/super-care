---
phase: 03-identity-vehicle-capture-fakes
plan: 06
type: execute
wave: 5
depends_on: ["03-05"]
files_modified:
  - apps/web/app/[locale]/intake/[id]/verify/page.tsx
  - apps/web/app/[locale]/intake/[id]/vehicle/page.tsx
  - apps/web/app/[locale]/intake/[id]/review/page.tsx
  - apps/web/src/components/intake/map-verify.tsx
  - apps/web/src/components/intake/vehicle-form.tsx
  - apps/web/src/components/intake/review-save.tsx
  - apps/web/src/server/actions/intake.actions.ts
  - apps/web/e2e/intake-happy-path.spec.ts
  - apps/web/e2e/verify-gate.spec.ts
  - apps/web/messages/en.json
autonomous: false
requirements: [CMI-04, CUST-07, VEH-01, UI-01, API-01]
user_setup: []

must_haves:
  truths:
    - "Map&Verify shows raw OCR beside the mapped suggestion with per-field Verified; Continue is disabled until all required fields verified"
    - "Vehicle step captures plate/chassis/engine validated by the pure validators"
    - "Review&Save persists the intake via intake.service and shows a success toast"
    - "The happy-path e2e passes end-to-end; a verify-gate e2e proves Continue/Save is blocked when a required field is unverified"
    - "The wizard is mobile-responsive (Map&Verify rows become stacked cards; sticky primary action)"
  artifacts:
    - path: "apps/web/src/components/intake/map-verify.tsx"
      provides: "raw|mapped rows with per-field verify (CMI-04 + CUST-07 UI)"
    - path: "apps/web/e2e/verify-gate.spec.ts"
      provides: "e2e proving the verify gate blocks advancement"
    - path: "apps/web/src/components/intake/review-save.tsx"
      provides: "summary + Save calling saveIntake"
  key_links:
    - from: "review-save"
      to: "intake.service.saveIntake"
      via: "saveIntakeAction (thin)"
      pattern: "saveIntake"
    - from: "vehicle-form"
      to: "@super-care/shared/validators"
      via: "zodResolver refine (plate/chassis/engine)"
      pattern: "isValid(Plate|Chassis|Engine)"
---

<objective>
Complete the wizard vertical slice: Map&Verify (raw OCR beside mapped suggestion, per-field
verify, Continue disabled until all required verified), Vehicle (plate/chassis/engine validated by
the pure validators), and Review&Save (transactional save via intake.service). Make the happy-path
e2e pass end-to-end and add a verify-gate e2e proving advancement/save is blocked when a required
field is unverified. Ensure mobile responsiveness (UI-01).

Purpose: This closes the slice — one staff intake captured end-to-end on fakes, with the
server-enforced verify gate surfaced in the UI. Exit gate: human-verify gate blocks advancement
without verifiedBy (now proven in the browser too).
Output: steps 4–6 + save wiring + passing happy-path e2e + verify-gate e2e + mobile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-identity-vehicle-capture-fakes/03-CONTEXT.md
@.planning/phases/03-identity-vehicle-capture-fakes/03-UI-SPEC.md
@.planning/phases/03-identity-vehicle-capture-fakes/03-RESEARCH.md
@.planning/phases/03-identity-vehicle-capture-fakes/03-VALIDATION.md
@CLAUDE.md
@packages/shared/src/validators/index.ts
@apps/web/src/server/modules/intake/intake.service.ts
@apps/web/src/server/modules/intake/verify-gate.ts
@apps/web/src/server/modules/intake/intake.schema.ts

<interfaces>
<!-- From 03-04 intake.service: suggestMappings(draftId, rawOcr), setVerified(ctx, draftId, map),
     saveIntake(ctx, draftId). From 03-03: MapperProvider suggestions {id,label,score}. -->
<!-- REQUIRED_VERIFY_FIELDS (verify-gate.ts): national-ID/passport, first/last name, DOB, plate,
     chassis, engine. The UI "Continue/Save" disabled state must mirror this — but the SERVER gate
     is the real contract (saveIntake throws); the e2e proves both. -->
<!-- vehicleStepInput zod schema (intake.schema) refines plate/chassis/engine via shared validators. -->
<!-- Mobile rules (UI-SPEC): Map&Verify rows → stacked cards (raw above, control below);
     sticky bottom primary action; nav in a Sheet. shadcn: combobox, checkbox, table, card,
     dialog/sheet, badge, sonner. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map&Verify step (raw|mapped rows + per-field verify, gate-aware UI)</name>
  <files>apps/web/app/[locale]/intake/[id]/verify/page.tsx, apps/web/src/components/intake/map-verify.tsx, apps/web/src/server/actions/intake.actions.ts, apps/web/messages/en.json</files>
  <read_first>
    - .planning/phases/03-identity-vehicle-capture-fakes/03-UI-SPEC.md (Map & Verify — the core screen; Verify-gate state; responsive rows→cards)
    - apps/web/src/server/modules/intake/verify-gate.ts (REQUIRED_VERIFY_FIELDS)
    - apps/web/src/server/modules/intake/intake.service.ts (suggestMappings, setVerified)
    - .planning/phases/03-identity-vehicle-capture-fakes/03-RESEARCH.md (Pattern 3; mapper suggestions never auto-commit)
  </read_first>
  <action>
    Build the Map&Verify step (CMI-04 + CUST-07 UI): map-verify.tsx renders, for each field, the
    raw OCR value beside a master-table Combobox prefilled from suggestMappings (suggestions only —
    never auto-committed) plus an editable control and a per-field Verified checkbox. Identifier
    fields re-run the pure validators on edit (inline errors). "Continue" is DISABLED until every
    REQUIRED_VERIFY_FIELD is verified, with a visible reason listing what's unverified
    (verify-gate state per UI-SPEC). On verify changes → setVerified action persisting the verified
    map to the draft. Responsive: rows become stacked cards on mobile (raw above, control below);
    sticky bottom primary action. All strings via next-intl; master data in stored Thai. The
    disabled button is UX only — the server gate (03-04) remains the contract.
  </action>
  <verify>
    <automated>cd /Users/khamphone/Documents/CodingProject/Projects/super-care && pnpm typecheck && pnpm lint && pnpm --filter @super-care/web build 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - Each field shows raw OCR value + mapped suggestion Combobox + per-field Verified control.
    - Continue is disabled until all REQUIRED_VERIFY_FIELDS verified, with a visible reason.
    - Mapper values are suggestions only (not auto-committed); identifier edits re-run validators.
    - build/typecheck/lint green; no hardcoded strings.
  </acceptance_criteria>
  <done>Map&Verify core screen surfaces raw|mapped + per-field verify with gate-aware UI.</done>
</task>

<task type="auto">
  <name>Task 2: Vehicle step + Review&Save (transactional save)</name>
  <files>apps/web/app/[locale]/intake/[id]/vehicle/page.tsx, apps/web/src/components/intake/vehicle-form.tsx, apps/web/app/[locale]/intake/[id]/review/page.tsx, apps/web/src/components/intake/review-save.tsx, apps/web/src/server/actions/intake.actions.ts, apps/web/messages/en.json</files>
  <read_first>
    - .planning/phases/03-identity-vehicle-capture-fakes/03-UI-SPEC.md (Vehicle step field list; Review & Save; Toast feedback)
    - packages/shared/src/validators/index.ts (isValidPlate/Chassis/Engine)
    - apps/web/src/server/modules/intake/intake.service.ts (saveIntake)
    - apps/web/src/server/modules/intake/intake.schema.ts (vehicleStepInput)
  </read_first>
  <action>
    Vehicle step (VEH-01): vehicle-form.tsx is a client RHF form using zodResolver(vehicleStepInput)
    — plate prefix/number + plate province, brand/model/year, color, vehicle type, chassis, engine,
    engineCC, seats, weight — prefilled from the verified mapping. chassis/engine/plate validated by
    the shared pure validators via the schema .refine (inline errors block Continue). Submit →
    captureVehicleAction → /review.

    Review&Save step: review-save.tsx shows a summary (Customer + Document + Vehicle) using shadcn
    Table/Card; "Save intake" → saveIntakeAction → intakeService.saveIntake (ctx actor "system").
    On success show a Sonner success toast + a confirmation; on a VerifyGateError surface an Alert
    (the server rejected — should not happen if the UI gate held, but the server is the contract).
    No pricing/payment (Phase 4+). All strings via next-intl.
  </action>
  <verify>
    <automated>cd /Users/khamphone/Documents/CodingProject/Projects/super-care && pnpm typecheck && pnpm lint && pnpm --filter @super-care/web build 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - Vehicle form validates plate/chassis/engine via shared validators (invalid blocks Continue).
    - Review shows Customer + Document + Vehicle summary; Save calls saveIntake.
    - Success → toast + confirmation; server VerifyGateError surfaced as an Alert.
    - build/typecheck/lint green; no hardcoded strings.
  </acceptance_criteria>
  <done>Vehicle capture + Review&Save wired to transactional intake.service.saveIntake.</done>
</task>

<task type="auto">
  <name>Task 3: Happy-path e2e passes end-to-end + verify-gate e2e + mobile check</name>
  <files>apps/web/e2e/intake-happy-path.spec.ts, apps/web/e2e/verify-gate.spec.ts</files>
  <read_first>
    - .planning/phases/03-identity-vehicle-capture-fakes/03-VALIDATION.md (Per-Task Verification Map: UI-01 e2e; Phase gate)
    - .planning/phases/03-identity-vehicle-capture-fakes/03-UI-SPEC.md (full flow + responsive rules + verify-gate state)
    - apps/web/playwright.config.ts (chromium + mobile-viewport projects from 03-01)
    - apps/web/e2e/intake-happy-path.spec.ts (the failing scaffold from 03-05)
  </read_first>
  <action>
    Complete the happy-path e2e (apps/web/e2e/intake-happy-path.spec.ts): drive Start (pick seeded
    insurer + New) → Customer (seeded-valid identifiers) → Document (upload a small fixture file →
    raw OCR shows) → Map&Verify (accept suggestions, check all Verified) → Vehicle (valid
    plate/chassis/engine) → Review → Save, asserting the success toast/confirmation. It must now
    PASS end-to-end. Add apps/web/e2e/verify-gate.spec.ts: reach Map&Verify, leave a required field
    (e.g. chassis) UNverified, assert Continue/Save is blocked AND (driving directly) the server
    rejects — proving the gate. Run the mobile-viewport project to confirm the wizard renders
    stacked/responsive (UI-01). Ensure both specs run via the Playwright config (chromium + mobile).
  </action>
  <verify>
    <automated>cd /Users/khamphone/Documents/CodingProject/Projects/super-care/apps/web && pnpm exec playwright test e2e/intake-happy-path.spec.ts e2e/verify-gate.spec.ts --reporter=line 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - happy-path e2e passes Start→Review→Save (success toast) — `playwright test` green.
    - verify-gate e2e proves Continue/Save blocked when a required field is unverified.
    - mobile-viewport run renders the wizard responsively (Map&Verify as stacked cards).
  </acceptance_criteria>
  <done>Full wizard happy path + verify-gate block proven in the browser; mobile responsive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser verify UI → server save | Disabled button is UX; the server gate is the real boundary |
| mapper suggestion → committed value | Suggestions must require human selection/verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-20 | Tampering | bypassing the disabled Continue/Save (forged request) | mitigate | server saveIntake calls assertVerified + re-runs validators (03-04); e2e verify-gate proves rejection |
| T-03-21 | Tampering/Integrity | mapper suggestion auto-committed without human verify | mitigate | suggestions are selectable only; per-field Verified required before save (CMI-04 + CUST-07) |
| T-03-22 | Information Disclosure | PII/raw OCR surfaced/logged in client | mitigate | values rendered for staff only; no client logging of PII; uploads stay encrypted server-side |
</threat_model>

<verification>
- `pnpm typecheck && pnpm lint && pnpm --filter @super-care/web build` green.
- `pnpm test && pnpm test:int` green (unit + integration unchanged/passing).
- `playwright test` (happy-path + verify-gate, chromium + mobile) green.
</verification>

<success_criteria>
The full intake wizard (6 steps) works end-to-end on fakes: a staff user captures one intake from
Start to Save with the server-enforced verify gate; happy-path + verify-gate e2e green; mobile
responsive. Phase 3 exit gate met.
</success_criteria>

<output>
Create `.planning/phases/03-identity-vehicle-capture-fakes/03-06-SUMMARY.md` when done.
</output>
